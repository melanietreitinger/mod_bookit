# These templates are <tr> row partials.
# They are designed to be included inside a <table>, so standalone HTML validation
# will always report "Stray start tag tr" errors. HTML validation is skipped for them.
resource_category_row.mustache
resource_item_row.mustache
